CREATE TABLE [dbo].[k_m_plan_grid_size] (
    [id_plan_grid_size]    INT           IDENTITY (1, 1) NOT NULL,
    [value_plan_grid_size] NVARCHAR (25) NULL,
    CONSTRAINT [PK_k_m_plan_grid_size] PRIMARY KEY CLUSTERED ([id_plan_grid_size] ASC)
);

