CREATE TABLE [dbo].[k_m_filter_configuration_join_type] (
    [id]          INT            IDENTITY (1, 1) NOT NULL,
    [description] NVARCHAR (MAX) NOT NULL,
    CONSTRAINT [PK_k_m_filter_configuration_join_type] PRIMARY KEY CLUSTERED ([id] ASC)
);

